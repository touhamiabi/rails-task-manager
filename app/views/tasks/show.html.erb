<ul>
<h2> <%= @task.title %></h2>
<p><%= @task.details %></p>
<p> <%= @task.completed %></p>
</ul>

<%= link_to "Update task", edit_path  %> | <%= link_to "Delete", delete_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%> | <%= link_to "Home", tasks_path  %>
